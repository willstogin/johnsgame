import { Component } from '@angular/core';
import { AppService } from '../app.service';


@Component({
    selector: 'component-placeholder',
    template: `
    Hi mom, you found my placeholder (kappa)
    `
})

export class ComponentPlaceholder {

    constructor() {}
}